# Frontend environment example
# Copy this to ".env" or ".env.local" and set the API URL for your deployed backend
# When the backend is deployed to Render the URL will look like:
#   https://ordering-app-uu24.onrender.com/api

VITE_API_URL=
